.Button {
  @apply w-fit h-fit bg-primary text-neutral-50 text-base font-medium font-sans py-4 px-8 rounded-md cursor-pointer duration-300 ease-in-out transform;
  @apply hover:scale-105;
  @apply active:scale-95;
}
